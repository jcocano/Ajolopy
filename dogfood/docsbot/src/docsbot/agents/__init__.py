"""Agents that ship with the dogfood docs bot.

Sub-packages stay flat in v0.1 — a single :class:`DocsAgent` covers the
demo. Future variants (a memory-aware bot, a different model) would
land alongside ``docs.py``.
"""
