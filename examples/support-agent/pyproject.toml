[project]
name = "support-agent"
version = "0.0.1"
description = "Runnable companion to the Ajolopy 3-step killer demo tutorial (AJ-50)."
requires-python = ">=3.14"
dependencies = [
    "ajolopy",
    "pydantic>=2.13.4",
]

[dependency-groups]
dev = [
    "pytest>=8.3.0",
    "pytest-asyncio>=0.24.0",
]

[tool.uv]
package = true

[tool.uv.sources]
# Path-mounted editable dependency: keeps the example self-contained inside
# the Ajolopy repository so `uv sync` works on a fresh clone — no published
# PyPI release required while v0.1 is in development.
ajolopy = { path = "../..", editable = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/support_agent"]

[tool.pytest.ini_options]
minversion = "8.0"
testpaths = ["tests"]
pythonpath = ["src"]
asyncio_mode = "auto"
