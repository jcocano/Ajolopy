"""Agent package for the local-ollama example.

A single agent — :class:`~local_ollama.agents.reviewer.CodeReviewer` —
lives here. Future variants of the demo (e.g. swapping the model
string for a hosted prefix) would be siblings inside this package.
"""
