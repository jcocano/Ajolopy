"""``{class_name}Service`` — generated by ``ajolopy generate service {name}``."""

from ajolopy import Injectable


@Injectable
class {class_name}Service:
    """Application service for the ``{snake_name}`` domain."""

    def do_work(self, payload: str) -> str:
        """Perform the service's primary action. Replace with real logic."""
        return payload
