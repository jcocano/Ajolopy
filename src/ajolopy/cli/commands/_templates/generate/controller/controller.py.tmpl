"""``{class_name}Controller`` — generated by ``ajolopy generate controller {name}``."""

from ajolopy import Controller, Get, Post


@Controller("/{snake_name}")
class {class_name}Controller:
    """HTTP endpoints for the ``{snake_name}`` resource."""

    @Get("/")
    async def list_items(self) -> dict[str, list[str]]:
        """Return every ``{snake_name}`` item. Replace with real logic."""
        return {{"items": []}}

    @Post("/")
    async def create_item(self, payload: dict[str, str]) -> dict[str, str]:
        """Create a new ``{snake_name}`` item. Replace with real logic."""
        return payload
