"""``{class_name}`` agent — generated by ``ajolopy generate agent {name}``."""

from ajolopy import Agent, Tool


@Agent(
    model="claude-sonnet-4-7",
    system="You are {class_name}, a helpful assistant.",
)
class {class_name}:
    """The {snake_name} agent. Replace the docstring with the real role."""

    @Tool
    def echo(self, text: str) -> str:
        """Return ``text`` unchanged. Replace with real logic."""
        return text
