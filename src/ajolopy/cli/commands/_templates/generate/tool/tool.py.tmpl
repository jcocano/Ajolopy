"""Standalone ``{class_name}`` tool — generated by ``ajolopy generate tool {name}``.

Most tools live inside an ``@Agent`` class; use this template when you
want a reusable callable that wraps a single capability and you intend
to attach it to an agent later.
"""

from ajolopy import Tool


@Tool
def {snake_name}(text: str) -> str:
    """Return ``text`` unchanged. Replace with real logic."""
    return text
